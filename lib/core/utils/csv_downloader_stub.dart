/// Stub implementation — does nothing on non-web platforms.
void downloadCsvFile(String csvContent, String fileName) {
  // Not supported on mobile/desktop in this app.
  // The Admin portal is web-only, so this path should never be hit.
}
